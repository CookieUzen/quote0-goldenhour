[project]
name = "quote0-goldenhour"
revision = "1.0"

[profiles.default]
QUOTE0_API_KEY = { description = "Dot. app API key (dot_app_...)", required = true }
QUOTE0_DEVICE_ID = { description = "Quote/0 device ID from the Dot. app", required = true }

[profiles.development]
QUOTE0_API_KEY = { description = "Dot. app API key (dot_app_...)", required = true }
QUOTE0_DEVICE_ID = { description = "Quote/0 device ID from the Dot. app", required = true }
